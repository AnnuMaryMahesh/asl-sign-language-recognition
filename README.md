# asl-sign-language-recognition
Real-time ASL alphabet recognition using MediaPipe and 5 ML classifiers (KNN, SVM, Random Forest, Naive Bayes, K-Means). Achieves 96.35% accuracy with majority-vote smoothing.
